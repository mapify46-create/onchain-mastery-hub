# Editor
.vscode/
.idea/

# Node — o hub em si nao usa (index.html + ES modules puro). O desktop-app/
# usa Electron de verdade; o package-lock.json dele fica versionado de
# proposito, pra fixar as versoes exatas usadas no build.
node_modules/

# Build do app desktop (gerado por `npm run build` dentro de desktop-app/)
desktop-app/dist*/

# Sistema operacional
Thumbs.db
Desktop.ini
.DS_Store
